services:
  joto-xiaoxin:
    build:
      context: .
    image: joto-xiaoxin:local
    container_name: joto-xiaoxin
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
      JOTO_DATA_DIR: /app/data/joto
    volumes:
      - ./data/joto:/app/data/joto
      - ./public/uploads:/app/public/uploads
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3000').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
