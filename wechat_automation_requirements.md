# 微信公众号自动化 — 需求文档

## 项目背景

基于 Leader 现有代码（JOTO 内容工厂）进行功能扩展。现有系统已支持：选产品 → 选角度 → 选风格 → 生成文章的四步流程，以及 AI 浓度检测、Dashboard 草稿箱管理。

目标是将现有的"手动辅助写作工具"升级为"半自动内容流水线"：用户只需做少量选择，系统批量生成多篇文章存入草稿箱，用户挑选满意的一篇进行排版后推送到微信公众号草稿箱，最后手动点击发布。

---

## 文章结构（固定骨架，所有角度通用）

每篇生成的文章必须包含以下三个部分，顺序固定：

1. **钩子** — 开门见山说明这个产品能带来什么业务效益，抓住读者注意力
2. **如何使用** — 简洁步骤说明，不需要长篇，让读者快速理解上手路径
3. **为什么选我们** — 对比传统方式或市面主流竞品，突出差异化优势

语气定位：不口语化、不过度学术，从业务角度出发，业务决策层和 IT 部门都能读懂。

---

## 文章角度（3个，替换原有10个）

三个角度共用上述固定结构，区别在于侧重点和读者定位：

| 角度 | 侧重 | 目标读者 |
|------|------|--------|
| 产品推广文 | 效益展示 + 使用场景 | 业务决策层、运营人员 |
| 深度对比文 | 竞品对比 + 技术差异分析 | IT 部门、技术评估者 |
| 活动/峰会文 | 行业背景 + 产品定位 | 会议受众、行业关注者 |

> 客户案例、功能解析、行业观察等内容不单独作为角度，由 AI 根据产品知识库自动决定是否作为正文小节融入，无需用户手动选择。

---

## 待开发功能（按优先级）

### 第一阶段：批量生成

**1. 角度改为多选**
- 现在：单选，只能选一个角度
- 目标：改为勾选框，支持同时勾选多个角度
- 角度选项替换为上方3个新角度

**2. 风格改为多选**
- 现在：单选，只能选一个风格
- 目标：改为勾选框，支持同时勾选多个风格

**3. 并行生成多篇独立文章**
- 现在：只生成1篇，正文内容不随角度/风格变化
- 目标：每个"角度 × 风格"组合各自独立调用 AI 生成一篇，内容完全不同
- 示例：勾选2个角度 + 3个风格 → 生成6篇独立文章，全部存入草稿箱
- 用户在 Dashboard 中挑选满意的一篇进行后续操作

---

### 第二阶段：排版 + 微信推送

**4. 微信公众号排版生成**
- 用户在草稿箱中点击某篇文章，进入排版页面
- 自动生成适合微信公众号的 HTML 排版，包括：
  - 花体标题字
  - 文字颜色和段落布局
  - 分割线、引用块等装饰元素
  - 文章内配图占位
- 排版结果可直接复制粘贴到微信公众号后台

**5. 文章配图**
- 根据文章内容自动匹配或生成配图
- 方案待定：AI 生成图片（DALL-E 等）或免费图库关键词搜索（Unsplash 等）

**6. 一键推送到微信草稿箱**
- 排版完成后，一键通过微信官方 API 推送到公众号草稿箱
- 使用认证服务号官方草稿接口（需 AppID + AppSecret）
- 零封号风险，用户在微信公众号后台手动点击发布

---

## 整体流程（目标状态）

```
用户操作：选产品 + 勾选多个角度 + 勾选多个风格
    ↓
系统并行生成 N 篇独立文章（角度 × 风格组合）
每篇结构：钩子 → 如何使用 → 为什么选我们
    ↓
全部存入草稿箱
    ↓
用户挑选一篇 → 点击"排版"
    ↓
自动生成微信排版 HTML（含配图）
    ↓
一键推送到微信公众号草稿箱
    ↓
用户在微信后台手动发布
```

---

## 当前进度

| 功能 | 状态 |
|------|------|
| 基础生成流程（4步向导） | ✅ 已完成 |
| AI 浓度检测 + humanize 优化 | ✅ 已完成 |
| Dashboard 草稿箱管理 | ✅ 已完成 |
| 角度改为多选（3个新角度） | 🔲 待开发 |
| 风格改为多选 | 🔲 待开发 |
| 并行生成多篇独立文章 | 🔲 待开发 |
| 微信排版生成 | 🔲 待开发 |
| 文章配图 | 🔲 待开发 |
| 一键推送微信草稿箱 | 🔲 待开发 |

---

## 备注

- 微信推送使用认证服务号官方 API，需提前准备 AppID 和 AppSecret
- 第一阶段完成后再启动第二阶段
- humanize 优化保留，但不需要循环迭代，一次生成 AI 浓度 &lt;40 即可通过
- 文章语气定位：业务视角，不口语化也不过度学术，业务层和 IT 层都能读懂
